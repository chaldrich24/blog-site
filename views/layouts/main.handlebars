<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Realest Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class='main-div'>
        <header>
            <a href='/'>
                <h1>
                    The Realest Blog Ever
                </h1>
            </a>
            <nav>
                <a href='/'>Home</a>
                {{#if loggedIn}}
                <a href='/dashboard'>Dashboard</a>
                <button type='click' id='logout-btn'>Logout</button>
                {{else}}
                <a href='/login'>Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>

    </div>
    <footer>
        Thanks for visiting!
    </footer>
    {{#if loggedIn}}
    <script src='/javascript/logout.js'></script>
    {{/if}}
</body>

</html>